{% extends 'base.html.twig' %}

{% block title %}VideoConference{% endblock %}

{% block body %}
    <h1>VideoConference</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ video_conference.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ video_conference.Nom }}</td>
            </tr>
            <tr>
                <th>Lien</th>
                <td>{{ video_conference.Lien }}</td>
            </tr>
            <tr>
                <th>IdReunion</th>
                <td>{{ video_conference.IdReunion }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ video_conference.code }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ video_conference.date ? video_conference.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('video_conference_index') }}">back to list</a>

    <a href="{{ path('video_conference_edit', {'id': video_conference.id}) }}">edit</a>

    {{ include('video_conference/_delete_form.html.twig') }}
{% endblock %}
